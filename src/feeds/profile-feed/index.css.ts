import styled from "@emotion/styled";

const ProfileFeedWrapper = styled.div`
  margin: 1rem 0;
`;

const ProfileHeadingWrapper = styled.div`
  display: flex;
  justify-content: space-evenly;
  margin: 2rem;
`;

export { ProfileFeedWrapper, ProfileHeadingWrapper };
